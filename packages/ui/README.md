# UI Design System (`@saki-operations/ui`)

Tokens, themes, and reusable components for Saki Operations.

- Dark theme is default
- Brand remaps via `data-brand` (`tours` | `hhco` | `office` | `admin`)
- Components accept labels as props — never hardcode user-facing copy
