# Internationalization (`@saki-operations/i18n`)

i18next-based locale foundation.

Supported locales:

- `en` — English
- `si` — Sinhala

Namespaces:

- `common` — shared product copy
- `ui` — design-system / chrome strings

Add module namespaces later as `locales/{en,si}/{module}.json`.
